# The deployed Cloudflare Worker that holds the Google refresh token.
# Public by design — it is an address, not a credential, and every request to
# it still has to carry the device key.
#
#   https://life-dashboard-api.<your-subdomain>.workers.dev
VITE_API_BASE=
